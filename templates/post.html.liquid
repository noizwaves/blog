<html>
<head>
  <title>{{ model.title }}</title>
</head>
<body>
  <header class="site-header">
    <a href="/">Adam Neumann's blog</a>
    <nav></nav>
  </header>
  <main class="post">
    <header>
      <h1>{{ model.title }}</h1>
      <div>Created at {{ model.createdAt}}</div>
      <div>Edited at {{ model.editedAt}}</div>
    </header>
    <article class="body">
      {{ model.bodyHtml }}
    </article>
  </main>
</body>
</html>

